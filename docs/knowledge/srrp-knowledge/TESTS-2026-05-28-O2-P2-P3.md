---
created: 2026-05-28
updated: 2026-05-28
tags: [test, sprint, ml, izohips, contour, sarimax, rcp]
---

# Test Planı — O1.4 + O2 + P2 + P3 (2026-05-28)

Bu dosya, **İzohips (O1.4/O2)** + **ML Projeksiyon (P2)** + **İklim Senaryoları
(P3)** sprintlerinin tüm doğrulama komutlarını tek yerde toplar. Tek seferde
koşup dönüt sağlamak için.

> Çalışma dizini varsayımları:
> - Backend komutları: `cd backend`
> - Frontend komutları: `cd frontend`
> - Backend venv: `.\venv\Scripts\python.exe` (Windows PowerShell)

İlgili planlar: [[PLAN-2026-05-27-IZOHIPS-ML]] · [[GRANULARITY-FORMULAS]]

---

## 0. Önkoşul — Backend ayakta mı?

```powershell
# Backend'i ayağa kaldır (ayrı terminalde, açık kalsın)
cd backend
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
```

Beklenen: `Application startup complete.` + `/ml/health 200`.

---

## 1. Backend — Import & Statik Doğrulama

```powershell
cd backend

# 1.1 Tüm yeni modüller import oluyor mu?
.\venv\Scripts\python.exe -c "from app.routers import contour_tiles, ml; from app.services.climate_scenarios import build_scenarios, scenarios_to_dict; from app.services.ml_sarimax_service import project_climatology; import app.main; print('ALL IMPORTS OK')"
```

**Beklenen:** `ALL IMPORTS OK`

---

## 2. P1/P2 — SARIMAX Forecast (mevcut, regresyon kontrolü)

```powershell
cd backend

# 2.1 SARIMAX validation — MAPE < %20 kabul kriteri
.\venv\Scripts\python.exe scripts\validate_sarimax.py

# 2.2 Tek il + metric
.\venv\Scripts\python.exe scripts\validate_sarimax.py --province Konya --metric sunshine
```

**Beklenen:** `2/2 test geçti (MAPE < 0.20)` (Rize/hydro climatology eksikse skip).

### 2.3 ML endpoint smoke (curl / tarayıcı)

```powershell
# Health
curl http://localhost:8000/ml/health

# Pin forecast (geçerli bir pin_id ile — örn. 1)
curl "http://localhost:8000/ml/project/pin/1?years=5"

# İl climatology forecast
curl "http://localhost:8000/ml/project/province/Konya?years=5&resource=solar&metric=sunshine"
```

**Beklenen:**
- `/ml/health` → `{"ready": true, "auto_arima": true, ...}`
- pin/province → `{target, points:[{date,value,lower,upper}], order, mape, ...}`

---

## 3. P3 — İklim Senaryoları (RCP4.5/8.5)

```powershell
# 3.1 climate_scenarios birim testi (saf fonksiyon — DB gerektirmez)
cd backend
.\venv\Scripts\python.exe -c "from datetime import date; from app.services.climate_scenarios import build_scenarios; pts=[(date(2026+i//12,(i%12)+1,1), 100.0) for i in range(24)]; r=build_scenarios(pts,'precipitation'); [print(k, round(v.points[-1].value,2), v.end_delta_pct) for k,v in r.items()]"
```

**Beklenen (yağış azalış senaryosu):**
- `baseline 100.0 0.0`
- `rcp45` son değer < 100 (negatif delta), `end_delta_pct` ≈ -0.3 × yıl
- `rcp85` rcp45'ten daha düşük (daha güçlü azalış)

```powershell
# 3.2 Scenario endpoint smoke
curl "http://localhost:8000/ml/scenario/province/Konya?years=10&resource=solar&metric=sunshine"
```

**Beklenen:** `scenarios:[{scenario:'baseline'...}, {scenario:'rcp45'...}, {scenario:'rcp85'...}]`, her biri `points:[{date,value}]`.

---

## 4. O2 — Self-hosted Contour MVT

```powershell
cd backend

# 4.1 Pipeline ortam kontrolü (gdal_contour, ogr2ogr, tippecanoe)
.\venv\Scripts\python.exe scripts\build_contour_mvt.py --check
```

**Beklenen:** Araç durum tablosu. Windows'ta tippecanoe genelde `BULUNAMADI`
(WSL/Linux gerekir) — bu **beklenen**; pipeline opsiyonel.

```powershell
# 4.2 Contour meta endpoint (mbtiles yokken graceful)
curl http://localhost:8000/api/v1/tiles/contour/meta
```

**Beklenen (mbtiles üretilmemişse):** `{"ready": false, "reason": "mbtiles bulunamadı", ...}`
→ frontend otomatik OpenTopoMap fallback yapar.

```powershell
# 4.3 (mbtiles üretildiyse) tek tile çek
curl -o tile.pbf "http://localhost:8000/api/v1/tiles/contour/10/600/380.pbf"
```

**Beklenen:** mbtiles varsa `200` + protobuf; yoksa `404`.

---

## 5. Frontend — Statik Analiz

```powershell
cd frontend
flutter analyze --no-pub
```

**Beklenen:** `No issues found!`

---

## 6. Frontend — Manuel UI Doğrulama (web)

```powershell
cd frontend
flutter run -d chrome
```

### 6.1 İzohips (O1.4/O2)
- [ ] Katmanlar paneli → **İzohips** toggle aç → kontur çizgileri görünür
- [ ] Görünürlük slider çalışıyor (0-100%)
- [ ] **Kaynak** toggle: `OpenTopo` ↔ `Kendi` geçişi
  - `OpenTopo` → OpenTopoMap raster (her zaman çalışır)
  - `Kendi` → mbtiles yoksa boş (beklenen); varsa kahverengi vektör çizgiler
- [ ] Attribution metni kaynağa göre değişiyor (`© OpenTopoMap` / `© SRRP / SRTM`)

### 6.2 ML Projeksiyon (P2)
- [ ] Raporlar → **Projeksiyon** (7. tab) açılıyor
- [ ] Mod: **Santral** → pin dropdown + horizon slider → chart (historical gri + forecast cyan + CI bandı)
- [ ] Mod: **İl İklim** → il dropdown + kaynak/metric chip + chart
- [ ] Yıllık KPI grid yıl toplamlarını gösteriyor
- [ ] Model parametreleri kartı (order/MAPE/method) dolu
- [ ] Horizon slider değişince chart güncelleniyor

### 6.3 İl Analizi Projeksiyon sub-tab (P2.5)
- [ ] Raporlar → **İl Analizi** → **Projeksiyon** sub-tab segmenti var
- [ ] Geniş ekranda (≥1100px) sadece "Projeksiyon" segmenti; seçince full-width
- [ ] Kaynak/metric/horizon seçilebiliyor, chart geliyor

### 6.4 İklim Senaryoları (P3)
- [ ] İl Analizi → Projeksiyon → en altta **İklim Senaryoları (RCP 4.5/8.5)** kartı
- [ ] Karta dokun → 3 çizgi (baseline düz cyan + RCP4.5 kesik amber + RCP8.5 kesik kırmızı)
- [ ] Her senaryo için legend + delta % + açıklama
- [ ] Kaynak/metric değişince (kart açıkken) yeniden yükleniyor

---

## 7. Native parite (O1.4) — opsiyonel cihaz testi

```powershell
cd frontend
flutter run -d <android-device-id>
```

- [ ] Katmanlar → İzohips toggle → OpenTopoMap raster native'de de görünür
- [ ] Opacity slider native'de çalışıyor (layer remove+add ile)

> Not: Native'de "Kendi" (self-hosted vektör) kaynağı şimdilik OpenTopoMap
> raster ile aynı davranır — native vektör contour gelecek polish (bkz.
> "Bilinen sınırlamalar").

---

---

## 8. Sprint M — ML İklim Projeksiyonu (batch + tematik harita)

### 8.1 Migration + tablo
```powershell
cd backend
..\.venv\Scripts\python.exe -m alembic current   # 017_ml_forecast olmalı
..\.venv\Scripts\python.exe -c "from app.db.models import MlForecast; print('MlForecast model OK')"
```
**Sonuç (2026-05-28):** ✅ tablo + 6 index, alembic 017'ye stamp'li.

### 8.2 Batch precompute
```powershell
cd backend
# Tek il (debug)
..\.venv\Scripts\python.exe scripts\build_ml_forecasts.py --province Konya --only-province
# Tüm iller
..\.venv\Scripts\python.exe scripts\build_ml_forecasts.py --only-province
```
**Sonuç:** ✅ 324 il serisi × 3 senaryo = ~117K satır. baseline<rcp45<rcp85.

### 8.3 Choropleth endpoint (canlı)
```powershell
curl "http://localhost:8000/ml/choropleth/sunshine/years?resource=solar"
curl "http://localhost:8000/ml/choropleth/sunshine?year=2035&resource=solar&scenario=baseline"
curl "http://localhost:8000/ml/choropleth/sunshine?year=2035&resource=solar&scenario=rcp85"
```
**Sonuç:** ✅ years 2026-2035; 2035 baseline 81 il (min 1385, max 1970, Konya 1741); rcp85 Konya 1788.

### 8.4 Pin finansal endpoint (canlı)
```powershell
curl "http://localhost:8000/ml/project/pin/1/financial?years=10"
```
**Sonuç:** ✅ endpoint çalışıyor (capex/opex/net/payback/co2 yapısı doğru).
**⚠️ Veri caveat:** <12 ay geçmişli pin'lerde climatology fallback güneşlenme
SAATLERİNİ kWh gibi döndürüyor → üretim ~400× düşük, payback hesaplanamıyor.
Spawn task ile ayrı görev açıldı (kWh birim dönüşümü).

### 8.5 Frontend analyze (M service layer)
```powershell
cd frontend
flutter analyze --no-pub
```
**Sonuç:** ✅ No issues found (MlChoropleth + MlPinFinancial DTO + servis metodları).

---

---

## 9. Sprint T — Tematik harita uzun pencereler (2y/5y/10y) + precompute

### 9.1 Migration + batch
```powershell
cd backend
..\.venv\Scripts\python.exe -m alembic current   # 018_thematic_aggregate
# İl + ilçe precompute (hibrit: yakın saatlik, eski günlük)
..\.venv\Scripts\python.exe scripts\build_thematic_aggregates.py
```
**Sonuç (2026-05-28):** ✅ il 2427 satır + ilçe ~32K satır. sixMonth/yearly/
twoYear → saatlik (1277 ilçe), fiveYear/tenYear → günlük (938 ilçe).

### 9.2 Precomputed serving endpoint (canlı — server gerektirir)
```powershell
curl "http://localhost:8000/weather/thematic-precomputed?mode=tenYear&level=province"
curl "http://localhost:8000/weather/thematic-precomputed?mode=season&season=summer&level=district"
curl "http://localhost:8000/weather/thematic-precomputed?mode=twoYear&level=district"
```
**Sonuç:** ✅ DB sorgu mantığı doğrulandı (server reload sırasında düştü → kullanıcı
restart etmeli). Değerler gerçekçi: rüzgar 5-14 m/s, yaz ışınımı 207-330 W/m²,
sıcaklık 2-24°C. Drop-in şekil: `{"İl|İlçe": {wind,solar,temp}, "_meta":{...}}`.

### 9.3 Frontend
```powershell
cd frontend
flutter analyze --no-pub
```
**Sonuç:** ✅ No issues found. Enum + dropdown 3 yeni seçenek (2/5/10 Yıl, ⚡ rozet);
`fetchDistrictChoropleth` precompute modlarını yeni endpoint'e yönlendiriyor.

### 9.4 Aylık scheduler
- `thematic_aggregate_refresh` job: her ayın 1'i 04:00 UTC (CronTrigger).
- Manuel: `build_thematic_aggregates.py` istenince çalıştırılabilir.

### 9.5 T-6 — Zaman simülasyonu uzun pencere (haftalık/aylık) ✅
```powershell
cd backend
..\.venv\Scripts\python.exe -m alembic current   # 019_thematic_timeseries
..\.venv\Scripts\python.exe scripts\build_thematic_timeseries.py
# Canlı endpoint:
curl "http://localhost:8000/weather/animation-precomputed?metric=temperature&period=month&years=5&scope=province"
```
**Sonuç (2026-05-28):** ✅ batch 428K satır (il aylık 104 dönem, ilçe aylık,
il haftalık 454 dönem). Endpoint mantığı doğrulandı: 5yr aylık temp = 44 frame,
Konya Mayıs 2021 = 18.43°C. Frontend: time sim panel'e Haftalık ⚡ / Aylık ⚡
interval + "Son 2/5/10 Yıl" seçici; controller precompute endpoint'e yönlendiriyor.
`flutter analyze` temiz.
- **Kaynak:** birim tutarlılığı için TEK kaynak (günlük weather_data). Son ~1 yıl
  province_name backfill boşluğu → o frame'ler seyrek (ayrı veri işi).
- **Kapsam:** monthly il+ilçe, weekly sadece il (ilçe×haftalık pratik değil).
- **Scheduler:** thematic_aggregate job'u ile birlikte ayda bir çalışır.

---

## Bilinen Sınırlamalar / Eksikler

1. **O2 veri pipeline'ı** — `build_contour_mvt.py` hazır ama gerçek `.mbtiles`
   üretimi gdal + tippecanoe gerektirir (Windows'ta tippecanoe yok). mbtiles
   üretilene kadar "Kendi" kaynağı boş döner, frontend OpenTopoMap kullanır.
2. **Native self-hosted vektör** — O1.4 raster parite tamam; native MVT vektör
   contour (LineStyleLayer + source-layer) gelecek iş.
3. **P3 downscaling kaba** — RCP deltaları sabit bölgesel katsayılar; tam
   çözünürlük için CMIP6/CORDEX/ERA5 verisi gerekir (gelecek iş).
4. **İl climatology 120-nokta** — gerçek 10y×12ay matrisi yerine 12-ay ortalama
   ×5 tekrar kullanılıyor (P3 ileri aşama: ERA5 historical import).

---

## Sonuç Tablosu (2026-05-28 otomatik koşum)

| Test | Komut/Adım | Sonuç |
|------|-----------|-------|
| 1.1 Backend import | `python -c "..."` | ✅ `ALL IMPORTS OK` |
| 2.1 SARIMAX validation | `validate_sarimax.py` | ✅ `2/2 geçti` (Konya 0.0112, Çanakkale 0.0000) |
| 2.3 ML endpoint smoke | curl health/province | ✅ health ready:true, province forecast 60pt + CI |
| 3.1 scenarios birim | `python -c "..."` | ✅ baseline 100 / rcp45 99.7 / rcp85 99.45 |
| 3.2 scenario endpoint | curl scenario | ✅ baseline+rcp45(+1.5%)+rcp85(+3.0%), 120pt |
| 4.1 contour env check | `build_contour_mvt.py --check` | ✅ çalışıyor (araçlar Windows'ta yok — beklenen) |
| 4.2 contour meta | curl meta | ✅ `ready:false` graceful (mbtiles yok) |
| 5 flutter analyze | `flutter analyze` | ✅ `No issues found!` |
| 6.1 İzohips UI | manuel | ☐ kullanıcı testi |
| 6.2 Projeksiyon UI | manuel | ☐ kullanıcı testi |
| 6.3 İl Analizi sub-tab | manuel | ☐ kullanıcı testi |
| 6.4 İklim senaryoları | manuel | ☐ kullanıcı testi |
| 7 Native parite | manuel cihaz | ☐ kullanıcı testi |

### Koşum sırasında bulunan + düzeltilen eksik

**`.venv` ortamında statsmodels/pmdarima yoktu.** Server repo kökündeki `.venv`'den
çalışıyor; kullanıcı paketleri `backend/venv`'e kurmuştu (iki ayrı ortam).
`/ml/health` lazy-import sayesinde çalışıyordu ama forecast/scenario endpoint'leri
modül-seviyesi `_SARIMAX_OK` flag'ini server başlangıcında `False` cache'lemişti.
Düzeltme: `.venv`'e de `statsmodels`+`pmdarima` kuruldu + `--reload` tetiklendi →
canlı forecast/scenario çalışır oldu.

> **Not:** Canlı `project_climatology` `method: sarimax_default` dönüyor (auto
> değil) — climatology 12-ay×5-tekrar serisinde auto_arima default order'a düşüyor.
> Forecast + CI geçerli; bu fallback zinciri tasarım gereği. Gerçek auto seçimi
> validate'teki sentetik trendli seride görülüyor.
